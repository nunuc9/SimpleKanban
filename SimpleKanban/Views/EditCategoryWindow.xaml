<Window x:Class="SimpleKanban.Views.EditCategoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Edit Category"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterOwner"
        MinWidth="300"
        MinHeight="150">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="Category Name" FontWeight="SemiBold" Margin="0,0,0,4"/>
            <TextBox Name="CategoryNameTextBox" Padding="4" Height="28"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Name="CancelButton" Content="Cancel" Click="CancelButton_Click" Margin="0,0,8,0" Width="80"/>
            <Button Name="OkButton" Content="OK" Click="OkButton_Click" IsDefault="True" Width="80"/>
        </StackPanel>
    </Grid>
</Window>
